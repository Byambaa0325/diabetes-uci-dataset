import pandas as pd


def build_features(df: pd.DataFrame) -> pd.DataFrame:
    """Return feature matrix X and target series y."""
    # TODO: encode categoricals, impute, scale numerics
    raise NotImplementedError
